let admin,User;_402‍.x([["currentUser",()=>currentUser],["privateRoute",()=>privateRoute]]);_402‍.w("../firebase",[["default",["admin"],function(v){admin=v}]]);_402‍.w("../models/user",[["default",["User"],function(v){User=v}]]);


       const currentUser = async (req, res) => {
  // console.log("CURRENT USER RETURNING FROM req.currentUser", req.currentUser);
  res.json(req.currentUser);
};

       const privateRoute = async (req, res) => {
  // console.log("REQ HEADERS TOKEN IN PRIVATE ROUTE", req.headers.token);
  if (req.currentUser) {
    res.json({
      ok: true,
    });
  } else {
    res.json({
      ok: false,
    });
  }
};
