let express,mongoose,cors,readdirSync;_b50‍.w("express",[["default",["express"],function(v){express=v}]]);_b50‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_b50‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_b50‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);



const morgan = require("morgan");

require("dotenv").config();

// app
const app = express();

// db
mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
    useFindAndModify: false,
    useCreateIndex: true,
  })
  .then(() => console.log(`DB Connected`))
  .catch((err) => _b50‍.g.console.log("DB connection error ", err));

// middlewares
app.use(cors());
app.use(morgan("dev"));

// routes middleware
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

// routes

const port = process.env.PORT || 8000;
app.listen(port, () => console.log(`Server is running on port ${port}`));
