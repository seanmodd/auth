let express,mongoose,cors,readdirSync;_43f‍.w("express",[["default",["express"],function(v){express=v}]]);_43f‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_43f‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_43f‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);



const morgan = require("morgan");

require("dotenv").config();

// app
const app = express();

// db
mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
    useFindAndModify: false,
    useCreateIndex: true,
  })
  .then(() => console.log(`DB Connected`))
  .catch((err) => _43f‍.g.console.log("DB connection error ", err));

// middlewares
app.use(cors());
app.use(morgan("dev"));

// routes middleware
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

// routes

const port = process.env.PORT || 8000;
app.listen(port, () => console.log(`Server is running on port ${port}`));
